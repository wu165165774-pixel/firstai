services:
  backend:
    build:
      context: ./backend

    container_name: novelforge-backend

    ports:
      - "18080:8000"

    env_file:
      - ./backend/.env

    volumes:
      - ./backend:/app

    restart: unless-stopped